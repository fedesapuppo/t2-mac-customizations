[Unit]
Description=Set power profile based on AC status at boot
After=power-profiles-daemon.service
Requires=power-profiles-daemon.service

[Service]
Type=oneshot
ExecStart=/usr/local/bin/power-config.sh

[Install]
WantedBy=multi-user.target
